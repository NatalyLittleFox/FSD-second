mixin toggle(options)
    .d-flex
        .toggle
            input(type="checkbox" class="toggle__button" id= options.name checked= options.checked)
            label(for= options.name class="toggle__label")
                span
        .toggle-text 
            span.toggle__text__content= options.text